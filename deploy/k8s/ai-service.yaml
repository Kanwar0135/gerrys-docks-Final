apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
    spec:
      containers:
        - name: ai-service
          image: gerrydocksacr.azurecr.io/ai-service:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 5004
          envFrom:
            - secretRef:
                name: gerrys-docks-secrets
          env:
            - name: AZURE_FOUNDRY_MODEL
              value: "gpt-5"
            - name: AZURE_FOUNDRY_AGENT_NAME
              value: "gerry-docks-ai"
            - name: AZURE_FOUNDRY_AGENT_VERSION
              value: "3"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
spec:
  type: ClusterIP
  selector:
    app: ai-service
  ports:
    - port: 5004
      targetPort: 5004